import React from 'react';
import { SizeContainer } from '../styling.jsx';

const SizeSelector = (props) => {
  return (
    <SizeContainer>Size Container Goes Here</SizeContainer>
  );
}

export default SizeSelector;